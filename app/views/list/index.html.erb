<h1>Lists</h1>

    <ul>
      <% @lists.each do |list| %>
        <li>
          <%= link_to list.title, list_to_do_list_index_path(list.id)%>
          <%= link_to 'Edit', edit_list_path(list.id)%>
          <%= link_to 'Delete', list_path(list.id), method: :delete, data: {confirm: "Você tem certeza?"}%>
        </li>
      <% end %>
    </ul>
    
    <%= link_to "New list of To-do", new_list_path%>