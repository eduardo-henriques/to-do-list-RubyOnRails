<h1>Lists</h1>

  <table class = 'table table-hover'>
    <thead>
      <tr>
        <th> Name</th>
        <th> Description</th>
        <th colspan='2'></th>
      </tr>
    </thead>

    <tbody>
      <% @lists.each do |list| %>
        <tr>
          <td><%= link_to list.title, list_to_do_list_index_path(list.id), class:"notlinker"%></td>
          <td><%= list.description %>
          <td><button type="button" class="btn btn-outline-warning"><%= link_to 'Edit', edit_list_path(list.id), class:"notlinker"%></button></td>
          <td><button type="button" class="btn btn-outline-danger"><%= link_to 'Delete', list_path(list.id), method: :delete, data: {confirm: "Você tem certeza?"}, class:"notlinker"%></button></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <button type="button" class="btn btn-outline-success"><%= link_to "New list of To-do", new_list_path, class:"notlinker"%></button>
    


  